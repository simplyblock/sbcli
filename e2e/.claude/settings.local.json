{
  "_comment": [
    "Per-developer paths. Gitignored: these are absolute and machine-specific.",
    "Committed template: settings.local.example.json",
    "",
    "Gives the agent file-tool access (Read/Grep/Glob) to the repos indexed by",
    "repo_map.py. Without this the maps still resolve message -> file:line, but",
    "reading the source needs Bash. Takes effect on the next session."
  ],
  "permissions": {
    "additionalDirectories": [
      "E:/sbcli",
      "E:/spdk",
      "E:/ultra",
      "E:/simplyblock-operator",
      "E:/documentation"
    ]
  }
}
