apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simplyblock-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /grafana
            pathType: Prefix
            backend:
              service:
                name: simplyblock-grafana
                port:
                  number: 3000
          - path: /graylog
            pathType: Prefix
            backend:
              service:
                name: simplyblock-graylog
                port:
                  number: 9000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: simplyblock-webappapi
                port:
                  number: 5000
