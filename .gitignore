/.idea/
*.pyc
e2e/logs/*.log!
.DS_Store
*.log
.python-version

# Ignore generated sbcli files
sbcli*
build
dist

.ruff_cache
.env
.tox

AGENTS.local.md

# Local-only agent configs, not shared via git
.claude/agents/

# Detached-deploy stderr capture and the per-deploy cluster metadata
# artifact (regenerated by every setup_perf_test_multipath.py run).
*.log.err
scripts/cluster_metadata_mp.json

# Locally collected incident/soak log bundles (collect_logs.py output,
# extracted tarballs, fio artifacts). Hundreds of MB; kept off the repo.
logs_*/
